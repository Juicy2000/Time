<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_time"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@android:dimen/dialog_min_width_major"
    android:paddingLeft="@android:dimen/dialog_min_width_major"
    android:paddingRight="@android:dimen/dialog_min_width_major"
    android:paddingTop="@android:dimen/dialog_min_width_major"
    android:orientation="vertical"
    tools:context="com.swufe.time.MainActivity">

    <EditText
        android:id="@+id/china"
        android:inputType="datetime"
        android:layout_width="match_parent"
        android:gravity="center"
        android:layout_height="wrap_content"
        android:hint="@string/china_hint_label" />

    <TextView
        android:id="@+id/show"
        android:gravity="center"
        android:textSize="35sp"
        android:layout_width="match_parent"
        android:text="@string/app_name"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/ameri"
        android:onClick="onClick"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_ameri_label"/>
    <Button
        android:id="@+id/jap"
        android:text="@string/btn_jap_label"
        android:onClick="onClick"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/eur"
        android:onClick="onClick"
        android:text="@string/btn_eur_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>